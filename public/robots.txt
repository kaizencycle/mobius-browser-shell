# Mobius Browser Shell — robots.txt
# C-367: Sitemap moved to canonical domain (was vercel.app — ignored by
# crawlers as unverified cross-host declaration). llms.txt added for
# GEO / LLM indexing. API routes remain blocked for all crawlers.

# Default: allow shell, block API
User-agent: *
Allow: /
Disallow: /api/

# AI crawlers — explicitly permitted for GEO / LLM indexing visibility.
# Each group must restate Disallow:/api/ because bot-specific groups do
# not inherit User-agent:* rules under the robots.txt spec.

User-agent: GPTBot
Allow: /
Disallow: /api/

User-agent: ClaudeBot
Allow: /
Disallow: /api/

User-agent: PerplexityBot
Allow: /
Disallow: /api/

User-agent: anthropic-ai
Allow: /
Disallow: /api/

User-agent: Googlebot
Allow: /
Disallow: /api/

User-agent: Applebot
Allow: /
Disallow: /api/

Sitemap: https://mobius-substrate.com/sitemap.xml

# AI-readable site map (llmstxt.org convention):
# https://mobius-substrate.com/llms.txt
