# Mobius Browser - Robots.txt
# Constitutional Principle: Transparency about what we want indexed

# Default: Allow indexing of public pages
User-agent: *
Allow: /

# Disallow indexing of development/staging labs until ready
Disallow: /citizen-shield
Disallow: /hive
Disallow: /wallet

# Disallow API endpoints (these shouldn't be indexed)
Disallow: /api/

# Allow search engines to find the sitemap (when we create one)
# Sitemap: https://mobius.systems/sitemap.xml

# Crawl-delay to be nice to servers (optional, not all crawlers respect this)
Crawl-delay: 1
